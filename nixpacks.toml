[phases.setup]
nixPkgs = ["...", "nodejs_20"]

[phases.install]
cmds = ["npm i -g pnpm && pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node dist/index.js"
